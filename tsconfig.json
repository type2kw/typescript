{ // typescript 컴파일 옵션
    "compilerOptions": {
        "module": "commonjs",
        "target": "ES2015", // 자바스크립트 버전
        "sourceMap": true,
        "outDir": "dist"
    },
    "include": ["src/**/*"], // 컴파일 과정에서 포함될 파일의 배열
    "exclude": ["node_modules"]  // 기본적으로 노드 모듈을 제외
}